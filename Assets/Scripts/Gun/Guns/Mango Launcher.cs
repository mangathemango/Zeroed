using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MangoLauncher : BaseGun
{

    // Start is called before the first frame update
    void Start()
    {
        SetupGun();
    }

    // Update is called once per frame
    void Update()
    {
        UpdateGun();
    }
}
